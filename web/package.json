{
  "name": "create-react-app",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "aws s3 cp ./build s3://www.cronin.nz/apps/whir/ --profile cronin --region us-west-2 --recursive"
  },
  "dependencies": {
    "@material-ui/core": "latest",
    "clsx": "latest",
    "firebase": "^8.2.1",
    "lodash": "^4.17.20",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "^5.2.0",
    "react-scripts": "latest",
    "recharts": "^1.8.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "babel-plugin-lodash": "^3.3.4"
  }
}
